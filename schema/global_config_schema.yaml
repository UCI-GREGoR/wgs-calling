$schema: "https://json-schema.org/draft/2020-12/schema"
description: global configuration
properties:
  manifest:
    type: string
  multiqc-config:
    type: string
  genome-build:
    type: string
    pattern: "^grch[0-9]+$"
  behaviors:
    type: object
    properties:
      aligner:
        type: string
        pattern: "^bwa-mem2$"
      snv-caller:
        type: string
        pattern: "^octopus$"
      sv-callers:
        type: array
        items:
          type: string
          pattern: "^manta$|^tiddit$"
      outcome:
        type: string
        pattern: "^fastqc$|^alignment$|^calling$"
      trim-adapters-before-alignment:
        oneOf:
          - type: boolean
          - type: string
            pattern: "^legacy$"
    required:
      - aligner
      - snv-caller
      - outcome
      - trim-adapters-before-alignment
    additionalProperties: no
  references:
    type: object
    patternProperties:
      "^grch[0-9]+$":
        type: object
        properties:
          fasta:
            type: string
          reportable-regions:
            type: string
        required:
          - fasta
          - reportable-regions
        additionalProperties: no
    required:
      - grch37
  dnascope:
    type: object
    patternProperties:
      "^grch[0-9]+$":
        type: object
        properties:
          model:
            type: string
          dbsnp-vcf-gz:
            type: string
          dbsnp-vcf-gz-tbi:
            type: string
    required:
      - grch37
  verifybamid2:
    type: object
    patternProperties:
      "^grch[0-9]+$":
        type: object
        properties:
          db-V:
            type: string
          db-UD:
            type: string
          db-mu:
            type: string
          db-bed:
            type: string
        required:
          - db-V
          - db-UD
          - db-mu
          - db-bed
        additionalProperties: no
    required:
      - grch37
  octopus:
    type: object
    patternProperties:
      "^grch[0-9]+$":
        type: object
        properties:
          forest-model:
            type: string
          error-model:
            type: string
          skip-regions:
            type: string
          calling-ranges:
            type: string
        required:
          - forest-model
          - error-model
          - skip-regions
          - calling-ranges
        additionalProperties: no
    required:
      - grch37
required:
  - manifest
  - multiqc-config
  - genome-build
  - behaviors
  - references
  - dnascope
  - verifybamid2
  - octopus
additionalProperties: no
