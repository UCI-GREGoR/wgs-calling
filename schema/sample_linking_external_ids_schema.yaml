$schema: "https://json-schema.org/draft/2020-12/schema"
description: an entry in the optional user-configured sample external ID linker
properties:
  sampleid:
    type: string
    description: "sample ID (corresponding column in sample manifest)"
  externalid:
    type: string
    description: "public-facing ID for sample export"
required:
  - sampleid
  - externalid
additionalProperties: no
